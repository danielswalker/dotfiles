Recommended debuggers:
CGDB for c/c++
pudb for python

Install linters:
pip install pylint
pip install flake8


Mac:

1.
brew install macvim --override-system-vim --with-lua --HEAD
brew unlink vim
brew install macvim --override-system-vim --with-lua --HEAD
brew link macvim
#brew install vim --with-lua

2.
solarize the terminal emulator by downloading this file:
https://raw.githubusercontent.com/jcberthon/solarized/b8f7484fed98de37736b7c025cbcea7647204fe6/osx-terminal.app-colors-solarized/Solarized%20Dark%20Yosemite.terminal

3.
in .vim/bundle
git clone https://github.com/altercation/vim-colors-solarized.git

4. YCM
installing YouCompleteMe and Ultisnips
brew install ctags
$ git submodule update --init --recursive
$ ./install.sh --clang-completer

remember Ctrl + Space, c only checks for semantic completion on things like . to avoid calling clang every keystroke

5.
Matlab
download https://www.mathworks.com/matlabcentral/fileexchange/21798-editing-matlab-files-in-vim
cd bundle/matlab
brew install dos2unix
cd ftplugin/
dos2unix matlab.vim
cd /usr/bin/
sudo ln -s /Applications/MATLAB_R2014b.app/bin/maci64/mlint mlint
make a tags file:
ctags -R .

6.
Tmux
brew install tmux
brew install reattach-to-user-namespace
cd ~/.vim/bundle
create ~/.tmux.conf:
